# Jonathan's Site

Make sure to install dependencies; development server is started at `http://localhost:4321`:

```bash
# npm
npm install
npm run dev
npm run build
npm run preview
```

```bash
# pnpm
pnpm install
pnpm dev
pnpm build
pnpm preview
```
